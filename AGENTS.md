# HRM 智能体协作规则

本文件是所有进入本仓库的智能体必须优先阅读的项目级规则。详细建设历程、决策依据、模块状态和经验记录见 [docs/project-memory.md](docs/project-memory.md)。

## 1. 工作原则

1. 先确认当前工作区、`main`、`origin/main` 和生产环境，再判断任务状态。
2. 严格区分四类证据：代码已实现、代码已提交、代码已上线、业务已验收。不得互相替代。
3. 需求含义、权限边界、生产数据或上线范围不明确时，先与用户确认；低风险且含义明确的工作直接完成并做一次针对性验证。
4. 只修改当前任务需要的文件。发现其他智能体或用户的未提交改动时，不覆盖、不回退、不顺手提交。
5. 结论必须附带新鲜证据。构建成功不等于业务流程通过，健康接口正常也不等于业务验收完成。

## 2. 数据权威边界

- HRM 员工档案和经确认的花名册是组织、人员、公司、部门、岗位、直属主管及任职状态的权威来源。
- 钉钉用于登录、外部身份绑定、工作台入口，以及后续通用 OA、考勤、消息和待办能力。
- 不得把钉钉通讯录或组织树作为自动覆盖 HRM 员工档案的来源。
- 导入花名册必须经过预览、校验、确认和审计；不得直接覆盖正式数据。
- 不在代码、日志、文档、截图说明或提交信息中保存密码、密钥、完整手机号、薪资明细等敏感信息。

## 3. 业务概念边界

- 岗位：描述员工做什么，不直接授予系统权限。
- 系统权限：控制系统管理和 HR 管理能力，由管理员维护。
- 直属主管：员工汇报关系，驱动团队范围、目标审核、月度评价等业务能力。
- 组织负责人：负责部门管理和组织范围，不自动等同于每名员工的直属主管。
- 上级审批人：负责指定审批环节，可继承或人工覆盖，不等同于直属主管或组织负责人。
- 绩效直属上级：考核周期启动时冻结到任务中的评价关系；后续组织调整不得静默改写已启动周期。
- 不得仅根据岗位名称、系统角色或“主管”字样推断业务权限。授权必须来自明确的业务关系或能力配置。

## 4. 架构边界

- 当前采用模块化单体：NestJS + Prisma + PostgreSQL + Redis + MinIO，前端为 Vue 3 + Element Plus，使用 Docker Compose 部署。
- 绩效目标、月度评价、算分、校准、审批、发布、申诉和审计共享事务与权限边界，现阶段不拆微服务。
- 只有出现独立团队、独立扩缩容、独立数据生命周期或明确的可靠性隔离需求，并且模块契约稳定后，才重新评估服务拆分。
- 新功能优先沿用现有模块、数据范围服务、审计、通知和工作流机制，不重复建设平行体系。

## 5. 需求与产品规则

- 需求打磨一次聚焦一个主题，先确认现状、痛点、角色、例外、数据来源、审批路径、报表和验收标准。
- 用户给出的成熟系统截图是结构和交互参考，不是本项目已实现功能的证据，也不是必须照搬的业务规则。
- 钉钉已有且适用的通用能力优先复用；HRM 自建公司专属的人事主数据、绩效规则、薪酬台账和专业分析。
- 招聘和薪酬当前是规划边界。未经业务确认，不把规划入口描述为已完成模块。
- 用户可见文案使用业务语言，操作结果和下一步必须明确；避免同义概念并存。

## 6. 前端体验规则

- 绩效和人事页面以安静、紧凑、可扫描、适合重复操作为目标。
- 列表默认简洁，详情按需展开；不要用大面积营销式卡片或装饰性视觉占用业务空间。
- 参考图中的页面层级、列表密度、筛选和展开方式应优先保持，但必须适配本项目真实数据和权限。
- PC 与手机端都要验证长文本、表格宽度、固定操作区、抽屉/弹窗和滚动行为。
- 表单错误应贴近字段显示；不只依赖顶部消息。按钮名称必须说明真实动作，不使用含义模糊的“确认”。
- 个人中心只展示与账号稳定关联的信息；周期角色、任务和动态职责留在对应工作台。

## 7. 开发和 Git 规则

1. 开始前运行 `git status --short --branch`，读取相关文件和近期提交。
2. 用户明确要求在领先的 `main` 上修改时，直接在 `main` 做最小变更；否则按任务风险决定是否隔离。
3. 不使用 `git reset --hard`、强制推送或会丢失未提交工作的命令。
4. 合并前检查 `git branch --no-merged main`、`git cherry main <branch>` 和所有已注册 worktree；旧分支可能是重复方案，不能仅凭“未合并”自动合并。
5. 提交时只暂存本任务文件，执行 `git diff --cached --check` 并核对暂存清单。
6. 推送后比较 `HEAD` 与 `origin/main`。TLS 等瞬时网络失败可以重试，但最终哈希一致才算推送完成。
7. 构建生成的声明、缓存和产物如果不属于任务，不得混入提交。

## 8. 测试与验收规则

- API 变更至少运行相关测试与 `npm run build`；共享流程、权限或数据模型变更应扩大到全量 API 测试。
- Web 变更至少运行 `npm run type-check` 或生产构建，并执行直接覆盖原问题的 Playwright 用例。
- 关键流程必须使用真实角色验收：员工、绩效直属上级、HR、审批人和系统管理员看到的范围与动作不同。
- 权限问题必须追踪真实请求、当前用户、业务关系、任务快照和后端数据范围，不能只修前端菜单。
- 测试账号能登录不代表已加入考核周期；必须继续验证参与范围、任务生成和完整业务流。
- 记录剩余风险。测试环境认证失效、第三方脚本警告或历史用例债务不得伪装成“全部通过”。

## 9. 生产发布规则

- 只有用户明确说“发布”或“上线”后才能变更生产环境。
- 正式地址为 `https://1260cw31az927.vicp.fun`，生产栈使用项目名 `kayford-deploy`。发布前仍需现场核对，不得只依赖本文。
- 发布前确认目标提交、改动服务、数据库迁移、外部健康状态、配置和回滚基线。
- 涉及数据库时先备份，再执行迁移；涉及容器时保留可识别的回滚镜像。
- 只替换需要发布的服务，不重建无关数据库、Redis 或 MinIO。
- 正式使用时 `ENABLE_TEST_QUICK_LOGIN` 必须关闭；验收临时开启也要有明确范围，并在交付前关闭。
- 发布后验证外部健康接口、迁移状态、生产资源内容和真实角色路径。容器启动不等于发布成功。

## 10. 版本规则

- 用户可见产品版本使用 `0.x`，前后端包使用对应 SemVer `0.x.0`。
- 当前源码版本为 `0.8`，包版本为 `0.8.0`。显示位置为“系统管理 -> 系统概览”。
- 版本号必须在 `README.md`、前后端 `package.json`/lockfile及系统概览中保持一致。
- 版本提交、版本上线和业务版本验收仍是三件不同的事；每次必须分别说明。

## 11. 停止并确认的情形

遇到以下情况不得自行扩大范围：

- 要修改真实员工、薪酬、组织或考核结果数据。
- 业务概念存在两种合理解释。
- 需要新增或扩大系统权限、审批权限或数据可见范围。
- 生产目标、回滚点、数据库迁移或第三方权限不清楚。
- 当前工作区存在会与本任务冲突的未提交修改。
- 测试结果与真实页面表现不一致。

## 12. 推荐工作顺序

`读取 AGENTS.md -> 查看 docs/project-memory.md -> 核对工作区和生产事实 -> 明确需求/边界 -> 实现 -> 针对性验证 -> 真实角色验收 -> 经授权发布 -> 外部复核 -> 更新状态记忆`
