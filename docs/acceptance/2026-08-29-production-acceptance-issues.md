# 2026-08-29 线上版本验收问题清单

## 记录规则

- 当前状态：ACC-001 已按用户指示汇集为一轮修改，完成开发、验证并发布到生产环境。
- 本文记录线上验收现象、原因、确认后的处理方式和验证证据。
- 本轮已于 2026-08-29 10:14（Asia/Shanghai）按用户明确指示发布，后续问题仍按“先记录、集中一轮修改、明确发布后上线”的规则处理。
- 后续线上验收问题继续追加，达到适合集中处理的数量后再形成下一轮修改。

## ACC-001 创建绩效周期：评分计划文案、默认操作与审核人选择

### 验收现象

1. 创建绩效周期弹窗中的提示、操作和月份卡片存在重复信息，视觉重点不清楚。
2. “保留特殊月份”“调整特殊月份”“重新应用默认规则”“恢复默认”“恢复全部默认”无法直接说明对象和影响范围。
3. 普通 HR 方园创建周期时，审核人下拉框没有可选项。

### 当前实现解释

1. “特殊月份”不是节假日月份，而是评分时间被人工改过的月份。修改该月的自评开放时间、员工计划完成时间或主管计划完成时间中的任意一项，系统就会把该月标记为特殊月份。
2. “调整特殊月份”当前只把该月标记为特殊月份，不会打开新的设置界面；而直接修改日期已经会自动完成相同标记，因此这个按钮没有独立业务价值。
3. 行内“恢复默认”只恢复当前月份；顶部“恢复全部默认”恢复全部月份。
4. “重新应用默认规则”会重新生成默认评分时间。勾选“保留特殊月份”时保留人工改过的月份；不勾选时会覆盖全部月份，与“恢复全部默认”功能高度重叠。
5. 审核人控件只查询“在职 HR 管理员”。方园拥有“周期计划创建编辑”能力，但通用人员查询仍叠加她的人员数据可见范围，因此 HR 管理员候选人被过滤为空。页面看起来像下拉框失效，实际是权限与候选查询口径不匹配。

### 本轮确认的业务处理

- 评分计划桌面端改为“一月一行”的紧凑列表；移动端保留纵向字段标签，避免窄屏横向溢出。
- “特殊月份”统一改为“已调整”，不再让人误解为节假日或特殊自然月。
- 删除无独立业务价值的“调整特殊月份”“保留特殊月份”“重新应用默认规则”。
- 仅在存在人工调整时显示“恢复本月默认”和“全部恢复默认”，明确恢复对象。
- 评分频率区只保留一次“结果统一按周期审核”，删除上下重复提示。
- 创建页取消审核人选择；新工作流周期进入 HR 管理员审核池，由实际处理的 HR 管理员领取并留下审核人记录。
- 历史周期已有审核人时继续沿用原审核人，不扩大方园的员工档案可见范围。

### 本轮已执行调整

1. 将“评分计划”区域压缩为简短说明、月份列表和必要操作，减少顶部说明与每月重复操作。桌面端改为“一月一行”的紧凑表格式：表头统一显示“月份 / 自评开放时间 / 员工计划完成时间 / 主管计划完成时间 / 状态与操作”，不在每个月重复字段标题；提醒只在对应月份行下方按需显示。移动端继续使用单月纵向卡片，保证日期输入可读、可操作。
2. 删除“调整特殊月份”按钮；用户直接修改时间后自动显示“已调整”标记。
3. 删除“保留特殊月份 + 重新应用默认规则”这一组难以理解的操作。
4. 仅在存在人工调整时显示恢复操作：单月显示“恢复本月默认”，顶部显示“全部恢复默认”；两者明确区分作用范围，并在执行前提示会覆盖哪些人工调整。
5. 审核流程采用“创建时不选审核人，提交后进入 HR 管理员审核池”；领取审核时通过版本号、原审核人和原审核状态一起校验，避免多人同时审核覆盖。
6. 已补充方园角色页面验收：保留周期创建编辑能力，创建弹窗不再出现审核人字段；未新增员工档案查看权限。

### 验证证据

- API：`cycles.service.spec.ts + launch.service.spec.ts`，79 项通过。
- Web：`25-cycle-scoring-plan.spec.ts + 14-cycle-management-compact.spec.ts`，39 项通过；包含桌面紧凑列表、390px 窄屏、方园创建页无审核人、HR 审核池和历史审核人兼容。
- 构建：API `nest build` 通过；Web `vue-tsc -b && vite build` 通过。
- 视觉检查：桌面渲染为统一表头加一月一行；直接修改后显示“已调整”，恢复操作只在需要时出现。
- 全量基线说明：API 全量为 603 通过、7 失败，失败均来自未改动的 `cycle-schedule.service.spec.ts` 时区期次偏移；Web 全量契约在运行页面用例前被 3 处旧夹具缺少 `planVersion` 的类型错误拦截。两类错误在本轮基准分支上可原样复现，不计为本轮回归通过，也不在本轮扩大修复范围。

### 当前状态

- 解释与根因：已完成。
- 方案：已确认并完成开发。
- 代码：已合并并推送到 `main`，功能提交为 `8c854de`。
- 发布：已于 2026-08-29 10:14（Asia/Shanghai）发布生产 API/Web；内外网健康接口、周期页面均返回 200，数据库状态为 `ok`，线上前端已确认包含本轮文案与紧凑列表资源。
- 数据边界：PostgreSQL、Redis、MinIO 容器未重建；测试快捷登录保持关闭。
- 回滚点：`kayford-deploy-api:rollback-before-acc001-20260829-100319`、`kayford-deploy-web:rollback-before-acc001-20260829-100319`。

## ACC-002 2027 工作日日历与自由排期

### 验收现象与确认口径

- 2026 年第四季度的评分计划会延伸到 2027 年，页面弹出“部分日期使用了工作日回退规则”，业务含义不直观且增加一次无必要确认。
- 用户确认：使用 2027 年日历生成默认时间，不再显示工作日回退、跨月或边界确认提示；人工调整时允许选择任意日期和时间，只保留节点先后顺序校验。
- 截至本轮发布，国务院尚未发布 2027 年年度放假调休通知；2027 年先按自然工作日及现行办法中日期确定的法定节日生成默认排期，不预设尚未公布的周末调休。正式通知发布后再维护调休日历，已保存日期不自动变化。

### 本轮已执行调整

1. API 与 Web 同步维护 2027 年基础工作日日历；2026 年 12 月评分默认生成自评开放 `2027-01-04 09:00`、员工截止 `2027-01-06 18:00`、主管截止 `2027-01-11 18:00`。
2. 删除“工作日回退规则”、考核期间边界提醒和评分计划二次确认弹窗；周末、跨月、较长间隔和不同月份评分计划重叠均可直接保存。
3. 页面统一显示“按系统工作日日历生成”，并明确人工调整可选择任意时间。
4. 保留自评开放、员工截止和主管截止的严格先后顺序校验；顺序错误仍会阻止保存并定位到对应月份。

### 验证与发布证据

- 测试驱动：API 新增行为测试 4 项、Web 新增页面测试 3 项，均先失败后通过。
- API：`cycles.service.spec.ts` 与 `launch.service.spec.ts` 通过；排期专项本轮 4 项通过。排期文件仍有 6 项既有本机时区期次偏移基线失败，不计为本轮回归通过。
- Web：两份相关契约共 47 项中 45 项通过，本轮新增 3 项全部通过；剩余 2 项为既有编辑场景未提交基线问题。
- 构建：API `nest build`、Web `vue-tsc -b && vite build` 通过。
- 发布：功能提交 `17ae0ba` 已推送 `main`，并于 2026-08-29 13:22（Asia/Shanghai）发布生产 API/Web；内外网健康接口和周期页均返回 200，数据库状态为 `ok`。
- 线上行为：生产 API 实算上述 2027 日期且提示数为 0；生产 Web 新文案存在，旧“工作日回退规则”和“确认评分计划提示”文案不存在。
- 数据边界：PostgreSQL、Redis、MinIO 容器未重建；测试快捷登录保持关闭。
- 回滚点：`kayford-deploy-api:rollback-before-2027-calendar-20260829-131819`、`kayford-deploy-web:rollback-before-2027-calendar-20260829-131819`。

## ACC-003 月度复盘与评分一体化

### 验收现象与业务问题

- “按月度评分”和“需要按月跟进”是两项配置，用户难以判断两者是否处于同一时间阶段、业务上应如何组合。
- 员工如果分别填写月度进展和月度自评，会重复录入同一批指标信息；主管评分也需要更适合 PC 和手机的快速处理方式。
- 旧页面的多组员工总结说明和主管评分说明不应继续作为强制项。

### 已确认口径

1. 新周期把月度跟进和月度正式评分合并为一个“每月复盘并评分”开关；开启后员工一次提交同时形成月度进展和自评，主管随后完成正式评分。
2. 月度周期固定开启，自定义周期固定关闭；季度、半年度和年度默认开启且允许关闭。旧草稿按 `scoringFrequency` 映射，已审核、已发起和执行中的历史周期不自动转换。
3. 员工 PC 端使用所有指标同屏表格，手机端使用逐项卡片；两端共用服务端草稿。完成度、进展状态和自评分必填，实际值、说明、附件和月度补充说明选填。
4. 主管 PC 端同屏评分、手机端逐项评分；“同意自评”只作用于当前指标，不提供一键填满全部指标。主管评分必填，主管说明选填；分差达到 10 分或主管分低于 60 分只提醒、不拦截。
5. 员工逾期后仍可提交，直到主管正式提交并锁定本期；主管退回原因选填。员工未提交不阻止主管到期后评分。
6. 自动保存需支持断网重试、多设备版本冲突和正式提交幂等；通知关闭时不发钉钉消息，但系统内待办仍按期次状态生成。

### 与既有计划的关系

- 以前已经确认并完成阶段 1 底座：评分频率、月度排期、期次和指标版本外壳。
- 以前也确认过阶段 2 的“员工月度自评、直属上级评分、有效月份等权汇总”方向。
- 本次新增并替代旧交互的内容是：取消独立月度跟进配置、把进展和自评合成一次填写、PC/手机分别采用表格/卡片、所有说明选填，以及草稿/幂等/兼容细则。

### 当前状态

- 设计：已确认，见 `docs/superpowers/specs/2026-08-29-unified-monthly-review-and-scoring-design.md`（提交 `63aa6c5`）。
- 实施计划：已形成，见 `docs/superpowers/plans/2026-08-29-unified-monthly-review-and-scoring.md`。
- 业务代码：尚未开发。
- 验证与发布：尚未进行；不得把本条记录描述为已上线。
